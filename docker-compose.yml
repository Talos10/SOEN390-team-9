version: "3.8"

services:
    frontend:
        build:
            context: ./frontend
        stdin_open: true
        ports:
            - 3000:3000
        env_file: ./env/frontend.env
        networks:
            - erp-app

    backend:
        build:
            context: ./backend
        ports:
            - 5000:5000
        depends_on: 
            - database
        env_file: ./env/backend.env
        networks:
            - erp-app

    database:
        image: mysql:latest
        command: --default-authentication-plugin=mysql_native_password
        restart: always
        ports:
            - 3306:3306
        env_file: ./env/database.env
        volumes: 
            - ./scripts/createNewDbUser.sql:/docker-entrypoint-initdb.d/createNewDbUser.sql
        networks:
            - erp-app

networks:
    erp-app:
        driver: bridge
